# 用0.5B模型实现HA智能家居控制？我用M5Stack LLM8850跑通了全流程

先说结论：**最终在qwen2.5-0.5B上实现了2秒内完成语音唤醒→识别→设备控制的完整链路**，包含ASR识别、停顿检测、数据传输的完整耗时。这个速度已经可以满足日常语音控制需求。

## 为什么要折腾小模型？

做Home Assistant（HA）控制时遇到个矛盾：大模型效果好但太慢，小模型快但不听话。我的目标是让1.5B甚至0.5B的模型也能像ChatGPT一样输出结构化指令，而不是随便吐几个字就完事。

核心思路很简单：**把现有设备信息注入LLM的上下文，让它输出标准格式的控制命令。** 但实际操作中踩了不少坑。

---

## 第一次尝试：提示词工程（失败）

最开始想靠提示词搞定，毕竟GPT那套工程学都验证过了。我在系统指令里明确给出了JSON格式示例：

![提示词示例](../../file/Pasted%20image%2020250522152939.png)

结果呢？**直接把示例里的内容原样输出，根本不听指挥。**

![错误输出1](../../file/Pasted%20image%2020251128111508.png)
![错误输出2](../../file/Pasted%20image%2020251128111525.png)

小模型的理解能力确实到不了大模型的水平，光靠提示词强行约束是行不通的。

---

## 第二次尝试：数据集微调（见效）

### 找到开源方案

翻GitHub时发现了 [home-llm](https://github.com/acon96/home-llm) 这个项目，作者已经用3B模型跑通了类似功能。他的核心方法是**构建专门的微调数据集**：

![数据集结构](../../file/Pasted%20image%2020251128111908.png)

- **系统指令**：随机组合设备+服务列表（模拟真实家庭场景）
- **用户指令**：从预置命令库中选择
- **模型输出**：匹配好的结构化JSON命令

这个思路很对，但我发现两个明显缺陷：

**❌ 缺陷1：没处理"设备不存在"的情况**  
比如说"打开卧室的洗衣机"，但系统里根本没这个设备，模型可能胡乱匹配个其他设备。

**❌ 缺陷2：模型不知道自己能控制啥**  
问它"你能控制什么设备"，模型答不上来，说明对设备列表的理解能力不足，导致实际控制时经常张冠李戴。

### 数据集改进方案

针对这两个问题，我扩展了训练数据：

**✅ 改进1：增加"设备未找到"样本**  
- 用户指令故意控制系统中不存在的设备
- 让模型回复统一的错误提示："devices not found"
- **实测效果明显**，垃圾文本基本都能过滤掉

**✅ 改进2：增加设备查询能力**  
- 添加"你能控制什么设备"类的询问样本
- 让模型学会从系统指令中提取并回复设备列表
- **隐约感觉设备名匹配更准确了**（虽然偶尔还是会串区域）

改进后的数据集和模型已开源：[qwen2.5-0.5b-ha @ HuggingFace](https://huggingface.co/yunyu1258/qwen2.5-0.5b-ha)

---

## 微调实战（用LLaMA-Factory）

整个过程用 [LLaMA-Factory](https://github.com/hiyouga/LLaMA-Factory) 来搞，步骤其实不复杂：

### 1️⃣ 转换数据集格式
把前面的JSON转成LLaMA-Factory要求的格式：

![格式转换](../../file/Pasted%20image%2020251128112833.png)

### 2️⃣ 注册数据集
修改 `dataset_info.json`，加入你的数据集配置：

![数据集注册](../../file/Pasted%20image%2020251128112921.png)

（这里的 `home_ass.json` 就是上一步转好的文件）

### 3️⃣ 修改身份预设
记得把库里预设的身份数据集改成你想要的名字（不然模型会自称ChatGPT 😂）

### 4️⃣ 开始微调
参考这个Colab笔记本： [微调脚本](https://colab.research.google.com/drive/1i_RSU8Y0EpkvfhfzVPPmuEvwcT_zIdxL?usp=sharing)

⚡ **关键参数**：
- 模型选择：qwen2.5-0.5b（平衡速度和效果）
- 显卡要求：建议3090以上，T4免费算力太慢了

---

## 实际部署架构

为了让这套系统真正能用，我写了个控制服务：[HomeAssistant-Edge](https://github.com/yuyun2000/HomeAssistant-Edge)

**整体流程：**
```
语音输入 → VAD语音检测 → KWS关键词唤醒 
→ ASR语音识别 → LLM结构化输出 → HA API控制
```

### 为什么选M5Stack LLM8850？

这套方案的关键硬件是 **M5Stack LLM8850加速卡**：

- ✅ **端侧推理能力**：0.5B模型推理时间控制在1.5s内
- ✅ **完整工具链**：支持VAD/KWS/ASR/LLM全套模型部署
- ✅ **局域网部署**：数据不出内网，隐私和响应速度都有保障
- ✅ **生态便利性**：M5Stack的模块化设计，后续扩展客户端很方便

**配置方式：**
- 输入HA的API密钥
- 加载预训练的ASR和LLM模型
- 启动局域网服务，等待其他M5设备接入

---

## 实测效果与问题

### 🎯 达成目标
- **响应速度**：2秒内完成完整控制流程（包含ASR+传输+停顿检测）
- **准确率**：常见设备控制基本准确
- **鲁棒性**：垃圾指令和不存在设备能正确拒绝

### ⚠️ 当前局限
1. **只在实验环境验证**：设备种类有限，真实场景需要更多测试
2. **客户端未完成**：目前只有8850做推理，带麦克风的M5模块接入代码还没写
3. **动态内容支持不足**：端侧推理为了提速把系统指令固定在内存里，导致无法实时获取设备状态、时间等动态信息

### 🔧 下一步优化方向
- **ASR容错性**：针对错别字、多音字增加样本
- **动态信息注入**：重构系统指令注入方式，支持实时状态查询
- **功能扩展**：定时器、场景联动等高级功能的数据集构建

---

## 总结

这次折腾的核心经验：**小模型不是不能用，而是要用对方法。** 提示词工程在大模型上有效，但小模型需要通过微调来"强制"学习特定任务模式。

M5Stack LLM8850在这套方案中起到了关键作用：既解决了端侧推理的算力问题，又通过本地部署保证了响应速度。如果你也想做类似的智能家居项目，这套硬件+软件的组合是个不错的起点。

项目还在迭代中，欢迎感兴趣的开发者一起改进 👉 [GitHub仓库](https://github.com/yuyun2000/HomeAssistant-Edge)

---

> 💡 **关键资源汇总：**
> - 微调数据集与模型：https://huggingface.co/yunyu1258/qwen2.5-0.5b-ha
> - 控制服务代码：https://github.com/yuyun2000/HomeAssistant-Edge  
> - 微调工具：https://github.com/hiyouga/LLaMA-Factory
> - 参考项目：https://github.com/acon96/home-llm