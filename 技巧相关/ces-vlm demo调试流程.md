开机后等待系统稳定（状态灯亮起），若vlm无画面输出且无文本输出；

进入金字塔的后台，首先运行命令，停止现有的vlm服务：
```
systemctl stop ha-vlm
```

然后依次运行：
```
cd /root/ha-server/ha-vlm
python3 vlm-server.py
```
手动运行vlm服务

查看命令行输出，判断服务状态：
1、服务卡在初始化，需要根据日志具体分析
2、取图失败，yolo视频流或者信道问题
3、vlm推理失败，vlm后端问题，需要进一步查看vlm日志
4、结果发送失败，或者结果发送成功但是ha接受失败，需要进一步查看ha的日志